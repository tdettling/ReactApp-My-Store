import { Text, View } from 'react-native'
import styles from '../../styles'

const ProductList = (props) => {
    return (
        <Text>
            Welcome to the Prodcut List Screen!
        </Text>
    );
};

export default ProductList;