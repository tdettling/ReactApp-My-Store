import { Text, View } from 'react-native'
import styles from '../styles'

const ProductDetails = (props) => {
    return (
        <Text>
            Welcome to the ProductDetails Screen!
        </Text>
    );
};

export default ProductDetails;